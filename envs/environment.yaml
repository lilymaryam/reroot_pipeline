name: reroot_env
channels:
  - conda-forge
  - bioconda
dependencies:
  - python=3.10
  - snakemake
  - numpy
  - pandas
  - pip
  - pip:
      - taxoniumtools==2.1.24
      - phylo-treetime
      - treeswift
